<% if @pull_request.body %>
<%= @pull_request.body %>

---------------------------------------------
<% end %>

You can view, comment on, or merge this pull request online at:

  <%= @pull_request.html_url %>

You were notified of this pull request because of the following changes:

<% @changes_view.each do |rules, changes| %>
  Changes monitored by <%= rules.ref %>/<%= rules.stewards_file %>:

  <% changes.each do |change| %>
    * <%= change.status_initial %> <%= change.file %> (<%= change.changes_count %>)
  <% end %>

<% end %>
---------------------------------------------
You received this email from Ladle for account <%= @user.github_username %>. Update your notification settings at <%= edit_user_settings_url %>.

Sent at <%= Time.now.to_s %>. Don't reply to this email. That would be silly.
